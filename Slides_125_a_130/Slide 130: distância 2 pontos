/*O algortimo faz a leitura de dois pontos quaisquer no plano, p1(x1,y1) e
p2(x2,y2) e calcule a distância entre eles.
Fórmula d = sqrt((x2-x1)**2+(y2-y1)**2) */

#include<stdio.h>
#include<math.h>
#include<locale.h>

main(){

setlocale(LC_ALL,"");
float x2,x1,y2,y1,d;

printf("Entre com os pontos x1, y1, x2 e y2. \n");
scanf("%f %f %f %f", &x1, &y1, &x2, &y2);

d = sqrt(pow(x2-x1,2)+pow(y2-y1,2));
printf("A distância entre os dois pontos é: %.2f. \n", d);


}
